import {eyebrow,button,icon,demo} from './shared.mjs';
export function ProblemSection(){return `<section class="problem section" id="why"><div class="container">${eyebrow('THE PROBLEM')}<div class="problem-intro reveal"><h2>Clinical trials<br>weren’t built to scale.</h2><p>Manual chart review, fragmented workflows, and labor-intensive follow-up slow trials down.</p></div><div class="problem-statements"><p class="reveal">Finding the right patients<br>is hard.</p><p class="reveal">Getting them to completion<br>is <em>harder.</em></p></div><div class="fragments reveal">${['Manual Chart Review','Fragmented Workflows','Labor-Intensive Follow-Up'].map(t=>`<div>${icon('document')}<span>${t}</span></div>`).join('')}</div><div class="connection-bridge reveal" aria-hidden="true"><svg viewBox="0 0 900 100" preserveAspectRatio="none"><path d="M150 0V20Q150 50 250 50H420Q450 50 450 100M450 0V100M750 0V20Q750 50 650 50H480Q450 50 450 100"/></svg><span>ClinTrial<span>AI</span></span></div><p class="problem-close reveal">ClinTrialAI turns manual trial workflows into<br><strong>intelligent, connected infrastructure.</strong></p></div></section>`;}
export function RetentionIndex(){return `<section id="retention" class="retention section"><div class="container"><div class="retention-layout"><div class="retention-copy reveal">${eyebrow('CLINTRIALAI RETENTION INDEX™')}<p class="section-kicker">RETENTION INTELLIGENCE FROM DAY ONE</p><h2>Recruit with<br><em>retention</em><br>in mind.</h2><p>The <strong>ClinTrialAI Retention Index™</strong> assesses dropout risk during recruitment and continues monitoring after enrollment—helping trial teams identify who may need additional support and intervene earlier.</p></div><div class="retention-product reveal"><div class="retention-dashboard"><div class="dashboard-header"><span>${icon('retain')} Retention intelligence</span><span class="concept-pill">CONCEPT / DEMO DATA</span></div><div class="patient-heading"><span class="avatar">${icon('person')}</span><div><strong>Patient 0241</strong><span>ClinTrialAI Retention Index™</span></div><span class="more" aria-hidden="true">···</span></div><div class="score-layout"><div class="score-ring"><svg viewBox="0 0 200 200" aria-hidden="true"><circle class="ring-track" cx="100" cy="100" r="82"/><circle class="ring-value" cx="100" cy="100" r="82"/></svg><div><strong data-score>72</strong><span>Retention Index</span></div></div><div class="score-note"><span class="risk-label"><i></i> Moderate risk</span><p>Recruit with<br>retention in mind.</p></div></div><div class="indicators">${[['Visit adherence','72'],['Travel burden','58'],['Engagement','83'],['Schedule complexity','64'],['Historical behavior','76']].map(([t,v],i)=>`<div><span>${t}</span><div class="indicator-track"><span class="indicator-fill indicator-${i}"></span></div></div>`).join('')}</div><div class="dashboard-bottom"><span class="status-dot"></span> Assess · Monitor · Intervene · Retain</div></div><p class="demo-note">Illustrative interface and demo data. Not a clinically validated prediction.</p></div></div><ol class="retention-process">${['Assess','Monitor','Intervene','Retain'].map((t,i)=>`<li class="reveal"><span>0${i+1}</span><strong>${t}</strong><b aria-hidden="true">↗</b></li>`).join('')}</ol><p class="retention-close reveal">Recruitment gets patients into trials.<br><strong>Retention gets trials to the finish line.</strong></p></div></section>`;}
export function TrialTeamSection(){return `<section id="team" class="team section"><div class="container team-layout"><div class="team-visual reveal"><div class="team-orbit" aria-hidden="true"></div><div class="team-center"><span>ClinTrial<span>AI</span></span><small>ONE INTELLIGENT LAYER</small></div><div class="team-person investigator">${icon('person')}<span>Investigator</span></div><div class="team-person coordinator">${icon('person')}<span>Coordinator</span></div><div class="team-person research-team">${icon('team')}<span>Research Team</span></div><span class="orbit-label">INTELLIGENCE AROUND YOUR TEAM</span></div><div class="team-copy reveal">${eyebrow('BUILT FROM INSIDE THE TRIAL')}<h2>We built AI around<br>the trial team.<br><em>Not the trial team<br>around AI.</em></h2><p>Designed around the real workflows of <strong>investigators, coordinators, and clinical research teams.</strong></p><p class="team-signoff">Built by trialists. Designed for trial teams.<br>Powered by AI.</p></div></div></section>`;}
export function ResearchEnterprise(){return `<section id="enterprise" class="enterprise section"><div class="container"><div class="enterprise-heading reveal">${eyebrow('BUILT FOR THE RESEARCH ENTERPRISE')}<p class="section-kicker">ONE PLATFORM. MULTIPLE TRIALS.</p><h2>One intelligent layer.<br><em>Your entire research enterprise.</em></h2><p>Built to work across investigators, coordinators, therapeutic areas, and trials—giving the health system a common execution layer for clinical research.</p></div><div class="enterprise-network reveal"><div class="enterprise-root"><span>ClinTrial<span>AI</span></span><span class="network-link"></span><div>${icon('building')} Health System</div></div><div class="trial-network">${['01','02','03','04'].map((n,i)=>`<div class="trial-card"><span class="trial-icon">${icon(['find','enroll','retain','complete'][i])}</span><h3>Trial ${n}</h3><div class="mini-records" aria-hidden="true"><span></span><span></span><span></span><span></span></div></div>`).join('')}</div><div class="enterprise-roles"><span>Investigators</span><span>Coordinators</span><span>Therapeutic Areas</span><span>Patients</span></div></div><p class="enterprise-close reveal">More patients. More trials. <em>Less manual work.</em></p></div></section>`;}
export function BrandMoment(){return `<section class="brand-moment"><div class="container"><div class="brand-label">FROM THE RIGHT PATIENT <span aria-hidden="true">↘</span></div><div class="brand-statements">${['Find faster.','Screen smarter.','Enroll better.','Retain longer.','Complete sooner.'].map((t,i)=>`<p class="brand-line reveal"><span>0${i+1}</span>${t}</p>`).join('')}</div><div class="brand-final reveal"><h2>One intelligent platform<br>for clinical trial execution.</h2><p>From the right patient to the completed trial.</p><span aria-hidden="true">↗</span></div></div></section>`;}
export function CTA(){return `<section id="contact" class="cta section"><div class="container reveal">${eyebrow('THE NEXT CHAPTER OF CLINICAL RESEARCH')}<h2>Ready to reimagine<br>clinical trial execution?</h2><p>Discover how ClinTrialAI can create one intelligent workflow<br class="desktop-break"> across your clinical research portfolio.</p><div class="actions">${button('Request a Demo')}${button('Talk to Our Team','mailto:info@clintrialai.com',true)}</div></div></section>`;}
